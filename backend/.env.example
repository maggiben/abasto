# Copy to backend/.env and adjust.
# Hybrid dev (USB printing on Mac/Windows): Postgres in Docker only — from repo root:
#   docker compose up -d postgres
# Then here: pip install -e ".[printer]" or ".[printer_escpos]" if PRINTER_PREFER_ESCPOS=true,
#   alembic upgrade head && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
# Frontend (separate terminal): cd ../frontend && cp .env.example .env.local && npm run dev
# Bootstrap admin user created on app startup if missing.
# Change these credentials immediately outside local development.
BOOTSTRAP_ADMIN_EMAIL=admin@abasto.com
BOOTSTRAP_ADMIN_PASSWORD=1234

# Admin product/inventory APIs require a JWT for a user with is_staff=true.
DATABASE_URL=postgresql+asyncpg://abasto:abasto@localhost:5432/abasto
JWT_SECRET=change-me-to-a-long-random-string
DEBUG=false

# Business panel: POST /admin/sales/reset-all (staff JWT) wipes POS tickets and web orders.
# Optional JSON body { "restore_inventory": true } to add sold quantities back to stock.
# Keep false in production; set true only for local demos / testing (then restart the API).
ALLOW_RESET_SALES=false

CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Optional: also allow localhost/127.0.0.1 on any port for local dev.
# CORS_ORIGIN_REGEX=^https?://(localhost|127\.0\.0\.1)(:\d+)?$
# Default sales tax % for POST /pos/checkout when tax_rate_percent is omitted (e.g. 10.5).
DEFAULT_TAX_RATE_PERCENT=0

# Thermal USB printer (same machine as API). Used for receipts and admin product labels.
# Install pyusb only (like root print.py):  pip install -e ".[printer]"
# Or pyusb + python-escpos (like easy.py): pip install -e ".[printer_escpos]"
# PRINTER_ENABLED=true
# PRINTER_USB_VENDOR=0x0483
# PRINTER_USB_PRODUCT=0x070B
# PRINTER_PROFILE=XP-58
# PRINTER_PREFER_ESCPOS=false
# RECEIPT_STORE_NAME=ABASTO
